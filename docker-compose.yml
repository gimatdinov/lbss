version: '2'

services:
    lbss:
        image: gimatdinov/lbss-sba
        ports:
            - 8080:8080
            - 2121:2121
        volumes:
            - lbssFTP:/data/lbss-ftp

    lbss-db:
        image: gimatdinov/lbss-db
        ports:
            - 27017:27017
        volumes:
            - lbssDB:/data/db
            
volumes:
    lbssFTP:
    lbssDB: