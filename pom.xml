<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<name>LBSS</name>
	<url>https://github.com/gimatdinov/lbss</url>
	<description>(L)oop(B)ack(S)mev(S)tub</description>
	<groupId>ru.otr.lbss</groupId>
	<artifactId>lbss</artifactId>
	<version>0.6.3</version>
	<packaging>pom</packaging>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<docker.image.prefix>localhost:5000</docker.image.prefix>
		<docker.images.version>${project.version}</docker.images.version>
	</properties>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>1.4.4.RELEASE</version>
	</parent>

	<modules>
		<module>lbss-util</module>
		<module>lbss-util-spring</module>
		<module>lbss-client</module>
		<module>lbss-service</module>
		<module>lbss-web</module>
		<module>lbss-sba</module>
	</modules>

	<profiles>
		<profile>
			<id>build-docker-images</id>
			<modules>
				<module>lbss-db</module>
			</modules>
		</profile>
		<profile>
			<id>push-docker-images</id>
			<modules>
				<module>lbss-db</module>
			</modules>
		</profile>
	</profiles>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-util</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-util-spring</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-client</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-service</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-web</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-sba</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>ru.otr.lbss</groupId>
				<artifactId>lbss-db</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>com.spotify</groupId>
					<artifactId>docker-maven-plugin</artifactId>
					<version>0.4.13</version>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>